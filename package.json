{
  "name": "nexus-site",
  "private": true,
  "description": "NEXUS product discovery platform (local dev)",
  "scripts": {
    "build": "node scripts/bundle.js",
    "prestart": "npm run build",
    "start": "serve .",
    "dev": "npm run build && serve ."
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
