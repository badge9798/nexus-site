[build]
  command = "npm run build"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA: all routes serve index.html (hash routing works client-side)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
